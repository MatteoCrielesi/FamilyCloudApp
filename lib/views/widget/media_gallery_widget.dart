import 'package:flutter/material.dart';

class MediaGalleryWidget extends StatelessWidget {
  const MediaGalleryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Galleria media'));
  }
}
